#include<bits/stdc++.h>
using namespace std;
int main(){
                                                             /*
    Arithematic Operator 
    It uses BODMAS Rules
    + (Addition)
    - (Subtraction)
    * (Multiplication)
    / (Division)
    % (Modulus)                                              */

    int a,b;
    cin >> a >> b;

    cout << a + b << endl;
    cout << a - b << endl;
    cout << a * b << endl;

    cout << a / b << endl;
    // dividing  two integer always gives back another integer.

    cout << a % b << endl;

    cout << (int)'a'<<endl;
    // Typecasting - convert one datatype into another

    cout << (char)'a' + 1 << endl;

    // Garbage Values (undefined values)
    cout << 'aa++' << endl;                                                     /*

 Precedence of the datatype:(Low to high)
 bool
 char / signed char / unsigned char
 short
 int
 long
 long long
 float
 double
 long double  
                                                                                */


}